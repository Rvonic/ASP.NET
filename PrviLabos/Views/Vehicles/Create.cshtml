@model PrviLabos.Models.VehicleCreateModel
@using PrviLabos.Model
@using PrviLabos.Models

@{
    ViewData["Title"] = "Novo vozilo";
}

<div class="ux-surface-card">
    <div class="ux-panel-header">
        <div>
            <h2 class="ux-section-title">Novo vozilo</h2>
            <p class="ux-muted">Unesi podatke za novo vozilo i dodijeli mu lokaciju ako treba.</p>
        </div>
        <a class="ux-text-link" asp-action="Index">Natrag na popis</a>
    </div>

    <form asp-action="Create" method="post" class="row g-3">
        @Html.AntiForgeryToken()

        <div class="col-md-4">
            <label asp-for="PlateNumber" class="form-label"></label>
            <input asp-for="PlateNumber" class="form-control" />
            <span asp-validation-for="PlateNumber" class="text-danger"></span>
        </div>

        <div class="col-md-4">
            <label asp-for="Brand" class="form-label"></label>
            <input asp-for="Brand" class="form-control" />
            <span asp-validation-for="Brand" class="text-danger"></span>
        </div>

        <div class="col-md-4">
            <label asp-for="Model" class="form-label"></label>
            <input asp-for="Model" class="form-control" />
            <span asp-validation-for="Model" class="text-danger"></span>
        </div>

        <div class="col-md-3">
            <label asp-for="ProductionYear" class="form-label"></label>
            <input asp-for="ProductionYear" class="form-control" type="number" min="1900" max="2100" />
            <span asp-validation-for="ProductionYear" class="text-danger"></span>
        </div>

        <div class="col-md-3">
            <label asp-for="Category" class="form-label"></label>
            <select asp-for="Category" asp-items="ViewBag.VehicleCategories" class="form-select"></select>
            <span asp-validation-for="Category" class="text-danger"></span>
        </div>

        <div class="col-md-3">
            <label asp-for="DailyRate" class="form-label"></label>
            <input asp-for="DailyRate" class="form-control" type="number" min="0" step="0.01" />
            <span asp-validation-for="DailyRate" class="text-danger"></span>
        </div>

        <div class="col-md-3">
            <label asp-for="CurrentMileage" class="form-label"></label>
            <input asp-for="CurrentMileage" class="form-control" type="number" min="0" />
            <span asp-validation-for="CurrentMileage" class="text-danger"></span>
        </div>

        <div class="col-md-6">
            @await Html.PartialAsync("_AutocompleteControl", new AutocompleteControlViewModel
            {
                Name = nameof(Model.CurrentLocationId),
                Id = "vehicle-current-location-id",
                Label = "Current location",
                Placeholder = "Search location...",
                SearchUrl = Url.Action("AutocompleteLocations", "Vehicles") ?? "/vozila/autocomplete/lokacije",
                SelectedValue = Model.CurrentLocationId,
                SelectedText = ViewBag.CurrentLocationDisplay as string,
                IsRequired = false
            })
        </div>

        <div class="col-md-3 d-flex align-items-end">
            <div class="form-check">
                <input asp-for="IsAvailable" class="form-check-input" />
                <label asp-for="IsAvailable" class="form-check-label"></label>
            </div>
        </div>

        <div class="col-12 d-flex gap-2">
            <button type="submit" class="ux-button-primary">Spremi</button>
            <a asp-action="Index" class="ux-button-secondary">Odustani</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
