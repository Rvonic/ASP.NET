@model IEnumerable<PrviLabos.Model.Customer>
@{
    ViewData["Title"] = "Customers";
}

@section Breadcrumbs {
    <nav class="ux-breadcrumb" aria-label="Breadcrumb">
        <a asp-controller="Home" asp-action="Index">Dashboard</a>
        <span>/</span>
        <span class="ux-breadcrumb-current" aria-current="page">Customers</span>
    </nav>
}

<div class="ux-location-page-actions">
    <a class="ux-button-primary" asp-action="Create">
        <span class="ux-button-primary-icon" aria-hidden="true">+</span>
        <span>New Customer</span>
        <span class="ux-button-primary-caret" aria-hidden="true">&#x2304;</span>
    </a>
</div>

<section class="ux-surface-card">
    <header class="ux-panel-header ux-location-panel-header">
        <div>
            <h2 class="ux-section-title">Customer Directory</h2>
        </div>
        <div class="ux-location-search-shell">
            <label class="visually-hidden" for="customer-search">Search customers</label>
            <svg class="ux-location-search-icon" aria-hidden="true" viewBox="0 0 24 24" fill="none">
                <path d="m21 21-4.35-4.35M10.5 18a7.5 7.5 0 1 1 0-15 7.5 7.5 0 0 1 0 15Z" />
            </svg>
            <input id="customer-search"
                   class="form-control ux-location-search ux-customer-search"
                   type="search"
                   aria-label="Search customers"
                   placeholder="Customer search"
                   autocomplete="off"
                   data-ux-customer-search />
        </div>
    </header>
    <div class="ux-table-wrap">
        <table class="ux-table" data-ux-customer-table>
            <thead>
                <tr>
                    <th>Customer</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Bookings</th>
                    <th>Tickets</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody data-ux-customer-table-body>
                @await Html.PartialAsync("_CustomerRows", Model)
            </tbody>
        </table>
    </div>
</section>

